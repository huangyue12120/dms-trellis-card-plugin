# Design Gate record

The Gate is recorded in `docs/ui-ux-spec.md` under `Design Gate record`.

User authorization on 2026-09-22 approved completing the two added usability
tasks plus the three original v0.5 tasks, with explicit instruction to explain
automatic detection/remembering clearly in settings and to use UI/UX skills
with restraint because this is not a full frontend design project.

Approved: native DMS, `auto` default, optional task/project/count/icon/full
projections, minimal read-only popout, trusted folder picker/list, bounded
depth-4 discovery, DMS-state summaries, and revalidation.

Not approved: whole-home guessing, emoji, rich task detail, project mutation,
custom themes, or decorative motion.
